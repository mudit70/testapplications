# 19-blog

A blogging platform (posts & comments).

| Tier | Stack |
|------|-------|
| Frontend | Vue (TS) + `fetch` |
| Backend | Laravel (PHP) |
| Data | Eloquent ORM (Postgres/MySQL) |

Plugins exercised: `vue`, `laravel` (+ `fetch`, `dom`).

```
adorable analyze 19-blog
```
