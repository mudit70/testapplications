# 18-banking

Retail banking app (accounts & transactions).

| Tier | Stack |
|------|-------|
| Frontend | React (TS) + `fetch` |
| Backend | Spring Boot (Java) |
| Data | JPA / Hibernate (Postgres) |

Plugins exercised: `react`, `spring`, `jpa` (+ `fetch`, `dom`).

```
adorable analyze 18-banking
```
